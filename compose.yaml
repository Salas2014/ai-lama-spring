services:
#  ollama:
#    image: 'ollama/ollama:latest'
#    ports:
#      - '11434:11434'
#    environment:
#      - OLLAMA_HOST=0.0.0.0
#    volumes:
#      - ollama_data:/root/.ollama
#
#  ollama-init:
#    image: 'ollama/ollama:latest'
#    container_name: ollama-init
#    labels:
#      - "org.springframework.boot.ignore=true"   # Spring Boot не пытается настроить connection details
#    environment:
#      - OLLAMA_HOST=ollama  # Говорим утилите pull стучаться в соседний контейнер
#    depends_on:
#      - ollama
#    entrypoint: [ "sh", "-c", "sleep 5 && ollama pull llama3.2:1b" ]

  prometheus:
    image: prom/prometheus:latest
    ports:
      - '9090:9090'
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    extra_hosts:
      - "host.docker.internal:host-gateway"

  grafana:
    image: grafana/grafana:latest
    ports:
      - '3000:3000'
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./docker/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - prometheus


  # http://localhost:6333/dashboard#/tutorial
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant-vector-db
    ports:
      - "6333:6333" # REST API и Web UI
      - "6334:6334" # gRPC (Spring AI по умолчанию использует его для скорости)
    volumes:
      - ./qdrant_storage:/qdrant/storage

volumes:
  ollama_data: